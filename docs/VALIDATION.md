# Validation — September 14, 2026

TypeScript, all 13 logic/database tests, and the production Expo web export pass. Focused Playwright coverage at a 390 × 844 phone viewport passes all three world/room framing checks, the labelled 3D medical and molecular viewers, the interactive calculus course, the Korean tracing/audio flow, the detailed RNA lesson, custom shopping palaces, five official SAT practice questions, and the complete six-stop palace recall flow.

The world map now uses an aspect-preserving cover fit so there are no side gutters on a phone. Every entered room uses its own square scene at full width and extends that same art as a blurred atmosphere behind the controls. The object cards preserve the source panel aspect ratio. The refreshed screenshots in this folder were captured from the tested mobile viewport.

The pi sequence is independently computed with Machin’s formula. Tests cover missing and extra digits, hidden pegs, 100-digit recall and persistence. The card cabinet now keeps stable rank and suit codes and adds an address and physical collision. Name-pair practice isolates the changed sound and tests retrieval in both directions. These choices follow the Major System, method of loci, image association, and feature-link guidance recorded in the product sources.

Medical cards now decode three visible story symbols into the mechanism before recall. The studio distinguishes schematic cell teaching models from the sourced detailed heart geometry and deposited experimental structures: human IgG PDB 1HZH and complement C9 PDB 5FMW. Camera AR still requires a compatible physical phone, HTTPS, and user-granted camera access. Android installation remains blocked by the host emulator/Gradle state; the verified mobile layout is the phone-sized live web preview rather than an installed APK.
