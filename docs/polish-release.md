# Preview polish

Removed the fictional-password badge and detailed revisit log. The progress dial uses a constrained, centred text column. Generated hanging flowers decorate the journal without covering controls. Global button text wraps inside its container. The secret-story thumbnail uses a dedicated source frame so the rocket remains visible.

Korean ieung now shows a learner watching a round moon from a summit. The explanation distinguishes its silent initial position from final ng.

## Artwork prompts

Built-in image generation, saved to `assets/korean-summit-moon.png`: Korean young adult with black hair, sage hanbok-inspired jacket, standing on a grassy summit looking at a round full moon; transparent background, Korean webtoon rendering, complete figure and moon with margins, no text.

Built-in image generation, saved to `assets/journal-flowers.png`: transparent horizontal botanical decoration with hanging jasmine, peach bell flowers, sage leaves and golden seed pods; hand-painted gouache, empty centre, no text or frame.

## Validation

TypeScript passed. All 13 data tests passed. Full browser run: 24 passed and two Chrome/startup timeouts. Targeted rerun: all three world-framing checks plus the new mobile progress/artwork test passed. The generated progress screenshot is 1179 × 2556 without a device frame; it depicts the browser build, not a native-device claim.

Cloud accounts, payments and device-only AR require their services or compatible hardware. These have not been verified as live integrations. See `next-gen-readiness.md` for submission dependencies.
